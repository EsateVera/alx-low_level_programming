#include "main.h"

/**
 * print_alphabet - entry level
 * descripion: 'prints the alphabet in lowercase'
 * Return: Always 0
 */
void print_alphabet(void)
{
	int v;

	for (v = 97; v <= 122; v++)
	_putchar(v);
	_putchar('\n');
}
